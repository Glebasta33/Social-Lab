<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.trusov.sociallab">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN"/>
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SocialLab">

        <receiver
            android:name=".feature_survey.data.receiver.NotificationAnswerReceiver"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".feature_survey.data.receiver.NotificationLaunchReceiver"
            android:enabled="true"
            android:exported="true" />

        <service android:name=".feature_survey.data.worker.NotificationLaunchService"
            android:exported="true"/>

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:authorities="androidx.work.WorkManagerInitializer"
            android:name="androidx.startup.InitializationProvider"
            tools:replace="android:authorities"
            tools:node="remove"/>
    </application>

</manifest>